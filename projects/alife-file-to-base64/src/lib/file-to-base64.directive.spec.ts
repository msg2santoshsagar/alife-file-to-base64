import { FileToBase64Directive } from './file-to-base64.directive';

describe('FileToBase64Directive', () => {
  it('should create an instance', () => {
    const directive = new FileToBase64Directive();
    expect(directive).toBeTruthy();
  });
});
